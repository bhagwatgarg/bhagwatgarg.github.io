import React from "react";
import "./achievements.css";

const Achievements = (props) => {
	return (
		<section id="achievements">
			<h1 className="section-head">My Achievements</h1>
		</section>
	);
};

export default Achievements;
